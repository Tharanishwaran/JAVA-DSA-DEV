package access_modifiers;

public class A {
    
   public int num; //using public keyword we can access outside the pakagae and outside the file and laso inside the file
   
    private String name; //using purivate keyword we can access only inside the file

   double numer; // without using any keyword than that is mean that deafult keyword so we only use this inside the pakage

   protected float fractionalnum; //using protected keyword we can use outside the pakage when the outside pakage file extends the protected file expect this we can use this like public
     


    





}
